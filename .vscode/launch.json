{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "FastAPI: Uvicorn (reload)",
      "type": "debugpy",
      "request": "launch",
      "module": "uvicorn",
      "args": ["src.app:app", "--reload", "--host", "0.0.0.0", "--port", "8000"],
      "jinja": false,
      "justMyCode": true,
      "envFile": "${workspaceFolder}/.env",
      "console": "integratedTerminal"
    }
  ]
}
